# 구글 드라이브 동기화용 OAuth 클라이언트 ID (선택 사항).
# 설정하지 않으면 드라이브 동기화만 비활성화되고, 나머지 기능은 정상 동작합니다.
# 발급 방법과 "승인된 JavaScript 원본" 설정은 README.md 참고.
#
# 이 값을 쓰려면 .env.local 파일을 만들어 복사하세요 (.env.local은 커밋되지 않습니다).
VITE_GOOGLE_CLIENT_ID="YOUR_GOOGLE_OAUTH_CLIENT_ID.apps.googleusercontent.com"
